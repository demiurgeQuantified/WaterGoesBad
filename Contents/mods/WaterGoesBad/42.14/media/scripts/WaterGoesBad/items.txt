module WaterGoesBad {
    item TapFilter {
        DisplayCategory = Water,
        Weight = 0.1,
        ItemType = base:normal,
        DisplayName = Tap Filter,
        /* FIXME: this looks really bad lol */
        /* FIXME: put this in a texture pack so it won't be filtered in the crafting menu */
        Icon = WaterGoesBad/TapFilter,
        StaticModel = WaterBottle,
        WorldStaticModel = WaterBottleEmpty,
        Tooltip = Tooltip_WaterGoesBad_TapFilter,
    }
}
